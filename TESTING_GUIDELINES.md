# 微信密友功能 - 测试大纲

## 1. 环境准备 (Environment Setup & Prerequisites)

*   **微信版本:**
    *   明确指定本插件模块测试所基于的微信版本号 (例如: WeChat vX.Y.Z)。
    *   记录任何已知的特定版本兼容性问题。
*   **WAuxiliary 插件框架状态:**
    *   确保 WAuxiliary 插件框架已正确安装并成功激活。
    *   确认框架本身运行稳定，无崩溃或错误日志。
*   **本插件模块状态 (密友插件):**
    *   确保“微信密友”功能模块已安装在 WAuxiliary 框架中，并已启用。
    *   检查插件模块是否有初始化错误日志 (通过 `PluginOtherMethod.log` 查看)。
*   **测试账号:**
    *   准备至少三个微信测试账号：
        *   账号 A: 发送方/主操作账号。
        *   账号 B: 接收方，将被指定为“密友”。
        *   账号 C: 普通好友/群聊，用于对比测试。
    *   准备一个测试群聊，其中包含账号 A 和 B。

## 2. 核心隐藏逻辑测试 (`ContactHidingHook.kt` 相关功能)

*   **正常模式下的隐藏效果:**
    *   **前置操作:**
        1.  通过设置界面激活“启用密友功能”。
        2.  设置一个“密友密码”。
        3.  进入“管理密友列表”，选择账号 B 和测试群聊作为隐藏对象并保存。
    *   **聊天列表测试:**
        *   返回微信主界面，检查与账号 B 的聊天会话、与测试群聊的会话是否已从聊天列表中消失。
        *   从账号 C 发送消息给账号 A，确认其会话正常显示。
    *   **通讯录测试:**
        *   进入通讯录，检查账号 B 是否已从好友列表中消失。
        *   检查测试群聊是否已从群聊列表中消失。
        *   确认账号 C 等普通好友和群聊正常显示。
    *   **搜索功能测试 (正常模式):**
        *   在微信主搜索框中搜索账号 B 的昵称或微信号，应无结果或结果中不包含账号 B。
        *   搜索测试群聊的名称，应无结果或结果中不包含该群聊。
        *   搜索账号 C，应能正常找到。
    *   **(重要提示)** 此部分隐藏功能的实现依赖于开发者通过通用方法Hook技术（如yukihookapi）对微信相关方法（如列表适配器、数据源获取方法）的正确Hook。测试时需关注这些Hook点的稳定性和准确性。

*   **密友模式激活与内容显示:**
    *   在微信主搜索框中输入之前设置的“密友密码”。
    *   **预期行为:**
        *   密码本身不应作为搜索关键词进行搜索。
        *   UI应有刷新迹象（或通过toast提示进入密友模式）。
        *   聊天列表: 重新检查聊天列表，此时应能看到与账号 B 和测试群聊的会话。
        *   通讯录: 重新检查通讯录，此时应能看到账号 B 和测试群聊。
        *   搜索功能 (密友模式): 再次搜索账号 B 或测试群聊，此时应能正常找到。
*   **密友模式下进行新搜索:**
    *   在已激活的密友模式下，在搜索框输入任意其他非密码的关键词 (例如账号 C 的昵称)。
    *   **预期行为:**
        *   应执行正常的搜索。
        *   搜索完成后，密友模式应自动退出，之前隐藏的好友和群聊应再次被隐藏。
        *   （可选）可以通过toast提示密友模式已退出。
*   **生命周期事件对密友模式的影响 (测试 `AppLifecycleHook.kt`):**
    *   **应用切换到后台:**
        1.  激活密友模式。
        2.  将微信切换到后台，然后再切换回前台。
        3.  **预期行为:** 密友模式应已自动退出，隐藏内容恢复隐藏状态。检查 `PluginOtherMethod.log` 中是否有相关日志。
    *   **摇一摇设备 (如果启用):**
        1.  激活密友模式。
        2.  执行摇一摇手机操作。
        3.  **预期行为:** 密友模式应已自动退出，隐藏内容恢复隐藏状态。检查 `PluginOtherMethod.log` 中是否有相关日志。

## 3. 设置界面与配置测试 (`SecretFriendsUIHook.kt`, `SecretFriendsSettings.kt`)

*   **访问设置界面:**
    *   确认在 WAuxiliary 插件框架的设置列表中能找到“微信密友”的入口。
    *   点击进入后，所有UI元素（标签、开关、按钮文字）均应为中文。
*   **主功能开关:**
    *   切换“启用密友功能”开关。
    *   退出设置界面再重新进入，检查开关状态是否已持久化 (通过 `PluginConfigMethod.getBoolean/putBoolean` 保存)。
    *   验证开关状态对核心隐藏逻辑的实际影响。
*   **密码设置:**
    *   输入新的“密友密码”并保存。
    *   退出设置界面再重新进入，或重启微信后检查密码是否已持久化。
    *   使用新密码测试密友模式的激活。
*   **管理密友列表功能:**
    *   点击“管理隐藏好友”按钮，应弹出“管理密友列表”对话框。
    *   **列表加载与显示:**
        *   对话框中应分别显示“好友列表”和“群聊列表”。
        *   验证列表内容是否通过 `PluginContactMethod.getFriendList/getGroupList` 正确加载。
        *   验证好友昵称/群聊名称是否正确显示 (依赖 `PluginContactMethod.getFriendName` 或Map中的显示名)。
        *   **空列表处理:** 如果好友或群聊列表为空，应显示相应的中文提示 (例如“好友列表为空或获取失败”)。
    *   **选择与保存:**
        *   勾选/取消勾选若干好友和群聊。
        *   点击“保存”按钮。
        *   应有“密友列表已更新”的toast提示 (通过 `PluginOtherMethod.toast`)。
        *   重新打开“管理密友列表”对话框，检查之前的选择是否已正确保存并反映在CheckBox的勾选状态上。
        *   验证 `SecretFriendsSettings.HiddenFriendIds` 是否已更新 (间接通过隐藏效果验证，或通过调试日志)。
    *   点击“取消”按钮，对话框应关闭，所做更改不应保存。
*   **自定义通知配置:**
    *   切换“启用密友自定义通知”开关，检查其状态是否持久化。
    *   修改“自定义通知声音 (URI或路径)”并保存，检查是否持久化。
    *   修改“自定义通知样式 (标识符)”并保存，检查是否持久化。
*   **来电拒绝配置:**
    *   切换“启用密友来电拒绝”开关，检查其状态是否持久化。
    *   **条件UI显示:**
        *   当总开关关闭时，下方的“拒绝模式”、“延迟时间”、“启用自动回复”、“自动回复内容”等设置应被隐藏。
        *   当总开关打开时，这些选项应显示。
        *   当“拒绝模式”选择为“延迟拒绝”时，“延迟时间”输入框应显示；选择“静默拒绝”时应隐藏。
        *   当“启用拒绝后自动回复”开关打开时，“自动回复内容”输入框应显示；关闭时应隐藏。
    *   修改各项配置（模式、延迟时间、自动回复开关、回复内容）并保存，检查是否持久化。

## 4. 自定义通知系统测试 (`NotificationHook.kt`, 由 `PluginCallback.onHandleMsg` 触发)

*   **前提条件:**
    *   在设置中启用“启用密友功能”和“启用密友自定义通知”。
    *   将账号 B 设置为隐藏好友。
    *   （可选）配置一个有效的“自定义通知声音URI”和一个可识别的“自定义通知样式”。
*   **来自隐藏好友/群的消息:**
    *   使用账号 C 给账号 A 发送一条普通消息。
        *   **预期行为:** 账号 A 收到正常的微信通知。
    *   使用账号 B 给账号 A 发送一条消息。
        *   **预期行为:**
            *   **原通知抑制:** 账号 A 不应收到来自微信的关于账号 B 这条消息的标准通知 (测试 `PluginMsgMethod.revokeMsg` 的效果)。
            *   **自定义通知显示:** 账号 A 应收到一个自定义样式的通知。
            *   **内容验证:** 自定义通知的标题应指明来自密友 (例如“来自密友: [账号B昵称/ID]”)，内容为消息文本。
            *   **声音验证:** 如果配置了有效的自定义声音URI，通知应播放该声音。否则，应为默认声音或无声 (取决于渠道和实现)。
            *   **样式验证:** 如果配置了特定样式 (如LED颜色、振动模式)，需目测或通过设备行为验证其效果 (注意：这部分依赖于`buildAndDisplayCustomNotification`中的具体实现和Android系统/设备支持)。
            *   **点击行为:** 点击自定义通知，行为应符合 `buildAndDisplayCustomNotification` 中 `contentIntent` 的设置 (例如，打开应用主界面或特定聊天界面 - 后者需开发者实现)。
*   **来自非隐藏好友的消息:**
    *   使用账号 C 给账号 A 发送消息。
    *   **预期行为:** 账号 A 收到标准的微信通知，不受自定义通知系统影响。

## 5. 来电拒绝功能测试 (`CallHook.kt`, 由 `PluginCallback.onHandleMsg` 中 `isVoip` 的 `MsgInfo` 触发)

*   **前提条件:**
    *   在设置中启用“启用密友功能”和“启用密友来电拒绝”。
    *   将账号 B 设置为隐藏好友。
*   **静默拒绝模式测试:**
    *   设置“拒绝模式”为“静默拒绝”。
    *   （可选）启用“自动回复”并设置回复内容。
    *   使用账号 B 向账号 A 发起语音或视频通话。
    *   **预期行为:**
        *   账号 A 的设备上，来电应被立即“拒绝”或“挂断”，不应有持续的响铃或完整的来电界面显示。(重要提示：此功能的完美实现依赖于开发者通过通用方法Hook对微信内部通话控制函数的精确操作。仅使用 `PluginMsgMethod.revokeMsg` 可能不足以完全抑制通话UI和响铃，主要效果可能是隐藏通话相关的系统消息。)
        *   如果启用了自动回复，在“拒绝”后，账号 B 应能收到预设的自动回复消息 (测试 `PluginMsgMethod.sendText`)。
*   **延迟拒绝模式测试:**
    *   设置“拒绝模式”为“延迟拒绝”，并设置一个延迟秒数 (例如5秒)。
    *   （可选）启用“自动回复”并设置回复内容。
    *   使用账号 B 向账号 A 发起语音或视频通话。
    *   **预期行为:**
        *   账号 A 的设备上，来电应先正常响铃/显示预设的延迟秒数。
        *   延迟时间到达后，如果通话未被接听，来电应被“拒绝”或“挂断”。(重要提示：同上，此功能的完美实现依赖通用Hook。`revokeMsg`可能仅影响消息层面。)
        *   如果启用了自动回复，在“拒绝”后，账号 B 应能收到预设的自动回复消息。
*   **功能禁用或非密友来电:**
    *   禁用“启用密友来电拒绝”开关，使用账号 B 呼叫账号 A。
        *   **预期行为:** 账号 A 正常接收来电。
    *   启用“启用密友来电拒绝”，但使用账号 C (非隐藏好友) 呼叫账号 A。
        *   **预期行为:** 账号 A 正常接收来电。

## 6. 整体稳定性与边界情况测试

*   **微信版本兼容性:**
    *   强调Hook的脆弱性，如果条件允许，在不同的小版本微信上进行抽样测试。
*   **并发操作:**
    *   快速、反复地修改设置。
    *   在设置过程中接收来自隐藏好友的消息或来电。
    *   在密友模式下快速进行多次不同搜索。
    *   测试网络断开和恢复时，各项功能的表现。
*   **日志检查:**
    *   在测试过程中，持续关注通过 `PluginOtherMethod.log` 输出的日志。
    *   检查是否有ERROR或WARN级别的意外日志，或与预期行为不符的INFO日志。
*   **“管理密友列表”用户体验:**
    *   列表加载速度（对于大量好友/群聊）。
    *   选择操作的流畅性。
    *   界面的直观性。
*   **资源消耗:**
    *   （可选，较高级）关注插件在激活各项功能后，对微信性能和耗电量的影响，尤其是摇一摇检测等持续运行的特性。

---
**测试结束标准:** 所有主要功能点符合预期，无严重错误或崩溃，用户体验在可接受范围内。已知问题和局限性应有记录。
